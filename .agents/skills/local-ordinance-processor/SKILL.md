---
name: local-ordinance-processor
description: "대한민국 지방자치단체의 조례·자치법규를 elis.go.kr/해당 의회에서 받아 메타데이터 주석이 포함된 마크다운으로 가공하고, 의미론적 카테고리(일반행정/보건_복지/교통_안전/산업_경제/도시_환경/교육_문화)별 폴더에 누적 저장한다. 개정 이력은 덮어쓰지 않고 append. Use when: 지자체 조례 수집·정리, 자치법규 마크다운 변환, 조례 분류·아카이빙."
---

# local-ordinance-processor

대한민국 지방자치단체 자치법규를 검색·분석에 최적화된 마크다운으로 변환하고 주제별로 누적 저장한다.

## 대상 지자체 식별

- 작업 시작 시 저장소 루트의 [`location.txt`](../../../location.txt)를 먼저 읽어 **대상 행정구역**을 확정한다.
- 본 스킬에서 `<지자체>`는 기초/광역 자치단체 이름을 가리킨다.
- 사용자가 채팅에서 다른 지자체를 명시하면 그 지시가 `location.txt`보다 우선한다.

## 워크플로우

### 1. 데이터 획득

- 우선 출처: `elis.go.kr` (자치법규정보시스템). 보조: 대상 지자체 의회·본청 공시.
- `gokr-fetch` 스킬로 원본을 `archive/raw/`에 보존(필수).
- 추출 대상: 조례 본문, 명칭, 시행일, 공포번호, 소관부서, 개정 연혁.

### 2. 마크다운 가공

- 파일 상단을 HTML 주석(`<!-- -->`)으로 감싸 메타데이터를 둔다(렌더링 시 숨김, grep 가능).
- 본문은 원문 텍스트를 보존하되, 「제○조」 단위 헤딩 정리는 허용.
- PII가 등장할 경우 `pii-mask` 스킬을 통과시킨 결과만 저장.

### 3. 주제별 분류

본문을 분석해 아래 카테고리 중 **하나**만 선택:

- `일반행정` — 시정 운영, 조직, 인사, 재정, 자산
- `보건_복지` — 복지 서비스, 보건소, 노인·아동·장애인 지원
- `교통_안전` — 도로, 교통망, 재난·안전, 소방, 보행
- `산업_경제` — 지역경제, 일자리, 기업 지원, 상업, 농업
- `도시_환경` — 도시계획, 주택, 환경 보호, 공원, 청소
- `교육_문화` — 교육 지원, 도서관, 예술, 관광, 체육

분류가 모호하면 사용자에게 확인을 요청한다(임의 결정 금지).

### 4. 저장 및 누적 (Incremental Accumulation)

- 경로: `data/processed/조례/<지자체>/<카테고리>/조례-<지자체>-<조례명>.md`
  (사용자가 다른 경로를 명시하면 그 경로 우선)
- 파일명 규칙: 공백·특수문자는 `_`로 치환, 「」 제거.
- **개정·변경 시 기존 내용을 덮어쓰지 않는다.** 파일 하단 `## 개정 이력` 섹션에 새 항목을 append.
- 동일 공포번호가 이미 있으면 중복 추가하지 않고 보고만 한다.

## 마크다운 표준 양식

```markdown
<!--
[요약 설명]
{조례의 목적과 주요 내용을 1~2문장으로 요약}

[법규 메타데이터]
- 지자체: {지자체명}
- 소관부서: {부서명}
- 시행일: {YYYY-MM-DD}
- 공포번호: {제0000호}
- 태그: {키워드1, 키워드2, ...}
-->

# {조례 명칭}

{원문 텍스트...}

## 개정 이력

- {YYYY-MM-DD} 공포 {제0000호}: {개정 요지}
  - 출처: {원본 URL} · sha256:{짧은해시}
```

## 완료 체크리스트

- [ ] 원본이 `archive/raw/`에 보존되고 SHA-256이 기록되었는가
- [ ] 메타데이터 주석에 지자체·소관부서·시행일·공포번호가 모두 채워졌는가
- [ ] 카테고리가 6종 중 하나로 결정되었는가(임의 신설 금지)
- [ ] 기존 파일이 있을 경우 덮어쓰기가 아니라 `## 개정 이력`에 append 했는가
- [ ] PII 마스킹·출처 footer가 포함되었는가

## 안전·정책

- 외부 호출은 `*.go.kr` 화이트리스트를 우선. 그 외 도메인은 명시 동의 후 사용.
- robots.txt·rate limit 준수. 위반 의심 시 중단하고 사용자에 보고.
- 정치적 해석·평가는 본문에 섞지 않는다(사실 카드만 작성).
