name: tools-gov-archive-tests

on:
  push:
    paths:
      - tools/gov-archive/**
  pull_request:
    paths:
      - tools/gov-archive/**
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tools/gov-archive

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
 
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          cache-dependency-glob: tools/gov-archive/pyproject.toml

      - name: "Install dependencies"
        run: |
          uv sync --dev

      - name: "Run tests"
        run: |
          uv run pytest -q tests
